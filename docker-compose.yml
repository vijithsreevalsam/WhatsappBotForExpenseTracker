version: '3.8'

services:
  whatsapp-bot:
    build: .
    container_name: whatsapp-expense-bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./auth_info:/app/auth_info
      - ./active_sheet.json:/app/active_sheet.json
